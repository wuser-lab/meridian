{
  "name": "meridian",
  "version": "1.0.0",
  "private": true,
  "description": "Documentation intelligence platform — lint READMEs and markdown for clarity, structure, and inclusivity",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
